name: moveto 28685
'on':
  push: null
jobs:
  build:
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v2
      - name: node
        uses: actions/setup-node@v1
        with:
          node-version: 12.x
      - name: 'npm:install'
        run: npm install
        env:
          CI: true
      - name: lint
        run: npm run lint --if-present
      - name: constants
        run: npm run constants
      - name: list
        run: npm run list
      - name: main
        run: npm run main
      - name: sleep for 10s
        uses: juliangruber/sleep-action@v1
        with:
         time: 10s
      - name: npm run details
        run: npm run details
      - name: sleep for 10s
        uses: juliangruber/sleep-action@v1
        with:
          time: 10s
      - name: npm run for sale
        run: npm run forsale
      - name: sleep for 10s
        uses: juliangruber/sleep-action@v1
        with:
          time: 10s
      - name: npm run summary
        run: npm run summary
      - name: upload artifact
        uses: actions/upload-artifact@v2
        with:
          name: constants
          path: data/*
      - name: commit data
        run: |
          git config --global user.email "xgirma@gmail.com"
          git config --global user.name "Girma Nigusse"
          git status
          git add --all
          git commit -am "generated 28685"
          git push
